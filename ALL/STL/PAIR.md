### PAIR
* 
```
pair <dataType1, dataType2> p;
Example: pair <int , pair<int, int>> p; 
int a = p.second.first;
Example: pair <int, int> p[100];
p.first  =1;
int arr[10];
arr[0]  = make_pair(1,2);
sort(p, p+size); //ascendeing sort
sort(p, p+size, comp); //descending sort

```
